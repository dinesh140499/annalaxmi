info:
  name: User Logout
  type: http
  seq: 6

http:
  method: GET
  url: "{{Local_URL}}auth/logout"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
