info:
  name: User Profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{Production URL}}user/profile"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
