info:
  name: Get All Admins
  type: http
  seq: 2

http:
  method: GET
  url: "{{Local_URL}}superadmin"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
