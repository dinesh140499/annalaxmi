info:
  name: Verify Otp
  type: http
  seq: 2

http:
  method: POST
  url: "{{Local_URL}}auth/verify-otp"
  body:
    type: form-urlencoded
    data:
      - name: phoneNo
        value: "7836008553"
      - name: otp
        value: "1234"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
