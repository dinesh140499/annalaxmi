info:
  name: Get All Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{Local_URL}}product?page=1&limit=10"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
